<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>gaborr</title>

  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      font-family: 'Outfit', sans-serif;
      background: black;
      overflow: hidden;
      color: white;
    }

    /* INTRO SCREEN */
    #intro {
      position: fixed;
      width: 100%;
      height: 100%;
      background: black;
      display: flex;
      justify-content: center;
      align-items: center;
      font-size: 20px;
      letter-spacing: 2px;
      cursor: pointer;
      z-index: 10;
      transition: opacity 0.8s ease;
    }

    /* VIDEO BACKGROUND */
    #video {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: none;
      z-index: -2;
    }

    /* DARK OVERLAY */
    #overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.55);
      display: none;
      z-index: -1;
    }

    /* CONTENT CENTERED */
    #content {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      display: none;
      z-index: 1;
      text-align: center;
    }

    /* TITLE */
    h1 {
      font-weight: 400;
      letter-spacing: 3px;
      font-size: 34px;
      margin-bottom: 30px;
    }

    /* LINKS (SIDE BY SIDE) */
    .links {
      display: flex;
      justify-content: center;
      gap: 30px;
    }

    .links a {
      color: white;
      text-decoration: none;
      font-size: 14px;
      letter-spacing: 2px;
      opacity: 0.8;
      transition: 0.2s;
    }

    .links a:hover {
      opacity: 1;
    }
  </style>
</head>

<body>

<!-- INTRO -->
<div id="intro">press to enter</div>

<!-- VIDEO -->
<video id="video" loop autoplay playsinline muted>
  <source src="video.mp4" type="video/mp4">
</video>

<!-- OVERLAY -->
<div id="overlay"></div>

<!-- CONTENT -->
<div id="content">
  <h1>gaborr</h1>

  <div class="links">
    <a href="https://steamcommunity.com/profiles/76561198873722287/" target="_blank">steam</a>
    <a href="https://www.instagram.com/gaborr69/" target="_blank">instagram</a>
    <a href="https://discordapp.com/users/1236036155955347537" target="_blank">discord</a>
  </div>
</div>

<!-- MUSIC -->
<audio id="music" loop>
  <source src="music.mp3" type="audio/mpeg">
</audio>

<script>
const intro = document.getElementById("intro");
const video = document.getElementById("video");
const overlay = document.getElementById("overlay");
const content = document.getElementById("content");
const music = document.getElementById("music");

intro.addEventListener("click", async () => {

  // fade intro out
  intro.style.opacity = "0";

  setTimeout(async () => {
    intro.style.display = "none";

    // show site
    video.style.display = "block";
    overlay.style.display = "block";
    content.style.display = "block";

    // play video (muted = no bug)
    video.play();

    // play music clean
    music.volume = 1;
    music.currentTime = 0;
    await music.play();

  }, 800);
});
</script>

</body>
</html>